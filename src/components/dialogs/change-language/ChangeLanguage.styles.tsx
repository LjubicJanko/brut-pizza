import { Dialog } from '@mui/material';
import styled from 'styled-components';

export const ChangeLanguageContainer = styled(Dialog)`
  align-items: center;

  .selected-language {
    background-color: wheat;
  }
`;
