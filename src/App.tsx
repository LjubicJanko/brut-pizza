import './App.css';

// const router = createBrowserRouter(routes);

function App() {
  return (
    <div className="main-container">
      App
      {/* <RouterProvider router={router} /> */}
    </div>
  );
}

export default App;
